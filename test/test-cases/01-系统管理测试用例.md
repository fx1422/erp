# 系统管理测试用例

**文档版本**: v1.0  
**创建日期**: 2026-04-03  
**测试阶段**: MVP 迭代 1

---

## 1. 用户登录/登出测试 (AUTH-001 ~ AUTH-005)

### AUTH-001 - 用户登录成功

| 项目 | 内容 |
|------|------|
| **用例 ID** | AUTH-001 |
| **用例名称** | 用户登录成功 |
| **优先级** | P0 |
| **前置条件** | 1. 用户已创建且状态为启用<br>2. 用户账号密码正确 |
| **测试数据** | 用户名：admin，密码：Admin@123 |
| **测试步骤** | 1. 访问登录页面 `/auth/login`<br>2. 输入用户名 `admin`<br>3. 输入密码 `Admin@123`<br>4. 点击登录按钮 |
| **预期结果** | 1. 登录成功，返回 JWT Token<br>2. 响应状态码 200<br>3. 响应体包含 token 和 user 信息 |
| **自动化脚本** | `test/automation/integration/auth.login.spec.ts` |

### AUTH-002 - 用户登录失败 - 密码错误

| 项目 | 内容 |
|------|------|
| **用例 ID** | AUTH-002 |
| **用例名称** | 用户登录失败 - 密码错误 |
| **优先级** | P0 |
| **前置条件** | 1. 用户已创建 |
| **测试数据** | 用户名：admin，密码：WrongPassword |
| **测试步骤** | 1. 访问登录接口 `POST /api/auth/login`<br>2. 输入正确用户名 `admin`<br>3. 输入错误密码 `WrongPassword`<br>4. 发送登录请求 |
| **预期结果** | 1. 登录失败<br>2. 响应状态码 401<br>3. 返回错误信息"密码错误" |
| **自动化脚本** | `test/automation/integration/auth.login.spec.ts` |

### AUTH-003 - 用户登录失败 - 账号不存在

| 项目 | 内容 |
|------|------|
| **用例 ID** | AUTH-003 |
| **用例名称** | 用户登录失败 - 账号不存在 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试数据** | 用户名：nonexistent，密码：AnyPassword |
| **测试步骤** | 1. 访问登录接口 `POST /api/auth/login`<br>2. 输入不存在的用户名 `nonexistent`<br>3. 输入任意密码<br>4. 发送登录请求 |
| **预期结果** | 1. 登录失败<br>2. 响应状态码 401<br>3. 返回错误信息"用户不存在" |
| **自动化脚本** | `test/automation/integration/auth.login.spec.ts` |

### AUTH-004 - 用户登录失败 - 账号已禁用

| 项目 | 内容 |
|------|------|
| **用例 ID** | AUTH-004 |
| **用例名称** | 用户登录失败 - 账号已禁用 |
| **优先级** | P0 |
| **前置条件** | 1. 用户已创建但状态为禁用 |
| **测试数据** | 用户名：disabled_user，密码：AnyPassword |
| **测试步骤** | 1. 准备禁用状态用户<br>2. 访问登录接口 `POST /api/auth/login`<br>3. 输入禁用用户的账号密码<br>4. 发送登录请求 |
| **预期结果** | 1. 登录失败<br>2. 响应状态码 403<br>3. 返回错误信息"账号已禁用" |
| **自动化脚本** | `test/automation/integration/auth.login.spec.ts` |

### AUTH-005 - 用户登出

| 项目 | 内容 |
|------|------|
| **用例 ID** | AUTH-005 |
| **用例名称** | 用户登出 |
| **优先级** | P1 |
| **前置条件** | 1. 用户已登录 |
| **测试数据** | 有效 JWT Token |
| **测试步骤** | 1. 使用有效 Token 访问登出接口 `POST /api/auth/logout`<br>2. 携带 Authorization 头<br>3. 发送登出请求 |
| **预期结果** | 1. 登出成功<br>2. 响应状态码 200<br>3. Token 被加入黑名单或失效 |
| **自动化脚本** | `test/automation/integration/auth.logout.spec.ts` |

---

## 2. 用户 CRUD 操作测试 (USR-001 ~ USR-009)

### USR-001 - 创建用户成功

| 项目 | 内容 |
|------|------|
| **用例 ID** | USR-001 |
| **用例名称** | 创建用户成功 |
| **优先级** | P0 |
| **前置条件** | 1. 管理员已登录<br>2. 用户名未被使用 |
| **测试数据** | `{username: "testuser01", password: "Test@123", email: "test@example.com", realName: "测试用户"}` |
| **测试步骤** | 1. 发送请求 `POST /api/system/users`<br>2. 携带管理员 Token<br>3. 请求体包含必填字段<br>4. 提交创建请求 |
| **预期结果** | 1. 用户创建成功<br>2. 响应状态码 201<br>3. 返回用户信息（不含密码）<br>4. 数据库中新增用户记录 |
| **自动化脚本** | `test/automation/integration/user.crud.spec.ts` |

### USR-002 - 创建用户失败 - 用户名重复

| 项目 | 内容 |
|------|------|
| **用例 ID** | USR-002 |
| **用例名称** | 创建用户失败 - 用户名重复 |
| **优先级** | P0 |
| **前置条件** | 1. 管理员已登录<br>2. 用户名已存在 |
| **测试数据** | `{username: "admin", password: "Test@123", email: "test2@example.com"}` |
| **测试步骤** | 1. 发送请求 `POST /api/system/users`<br>2. 使用已存在的用户名 `admin`<br>3. 提交创建请求 |
| **预期结果** | 1. 创建失败<br>2. 响应状态码 409<br>3. 返回错误信息"用户名已存在" |
| **自动化脚本** | `test/automation/integration/user.crud.spec.ts` |

### USR-003 - 创建用户失败 - 缺少必填字段

| 项目 | 内容 |
|------|------|
| **用例 ID** | USR-003 |
| **用例名称** | 创建用户失败 - 缺少必填字段 |
| **优先级** | P0 |
| **前置条件** | 1. 管理员已登录 |
| **测试数据** | `{username: "testuser02"}` (缺少 password 和 email) |
| **测试步骤** | 1. 发送请求 `POST /api/system/users`<br>2. 请求体缺少必填字段<br>3. 提交创建请求 |
| **预期结果** | 1. 创建失败<br>2. 响应状态码 400<br>3. 返回字段验证错误信息 |
| **自动化脚本** | `test/automation/integration/user.crud.spec.ts` |

### USR-004 - 查询用户列表

| 项目 | 内容 |
|------|------|
| **用例 ID** | USR-004 |
| **用例名称** | 查询用户列表 |
| **优先级** | P0 |
| **前置条件** | 1. 管理员已登录<br>2. 系统中存在用户数据 |
| **测试数据** | 无 |
| **测试步骤** | 1. 发送请求 `GET /api/system/users`<br>2. 携带有效 Token<br>3. 获取用户列表 |
| **预期结果** | 1. 查询成功<br>2. 响应状态码 200<br>3. 返回用户列表数组<br>4. 每个用户包含 id、username、email、roles 等信息 |
| **自动化脚本** | `test/automation/integration/user.crud.spec.ts` |

### USR-005 - 查询用户详情

| 项目 | 内容 |
|------|------|
| **用例 ID** | USR-005 |
| **用例名称** | 查询用户详情 |
| **优先级** | P0 |
| **前置条件** | 1. 管理员已登录<br>2. 用户已存在 |
| **测试数据** | 用户 ID |
| **测试步骤** | 1. 发送请求 `GET /api/system/users/:id`<br>2. 携带有效 Token<br>3. 传入有效用户 ID |
| **预期结果** | 1. 查询成功<br>2. 响应状态码 200<br>3. 返回完整用户信息<br>4. 包含关联的角色信息 |
| **自动化脚本** | `test/automation/integration/user.crud.spec.ts` |

### USR-006 - 查询用户详情 - 用户不存在

| 项目 | 内容 |
|------|------|
| **用例 ID** | USR-006 |
| **用例名称** | 查询用户详情 - 用户不存在 |
| **优先级** | P1 |
| **前置条件** | 1. 管理员已登录 |
| **测试数据** | 不存在的用户 ID (如 UUID 格式但数据库中不存在) |
| **测试步骤** | 1. 发送请求 `GET /api/system/users/:id`<br>2. 携带有效 Token<br>3. 传入不存在的用户 ID |
| **预期结果** | 1. 查询失败<br>2. 响应状态码 404<br>3. 返回错误信息"用户不存在" |
| **自动化脚本** | `test/automation/integration/user.crud.spec.ts` |

### USR-007 - 更新用户信息

| 项目 | 内容 |
|------|------|
| **用例 ID** | USR-007 |
| **用例名称** | 更新用户信息 |
| **优先级** | P0 |
| **前置条件** | 1. 管理员已登录<br>2. 用户已存在 |
| **测试数据** | `{email: "newemail@example.com", phone: "13800138000", realName: "新名字"}` |
| **测试步骤** | 1. 发送请求 `PATCH /api/system/users/:id`<br>2. 携带有效 Token<br>3. 请求体包含要更新的字段<br>4. 提交更新请求 |
| **预期结果** | 1. 更新成功<br>2. 响应状态码 200<br>3. 返回更新后的用户信息<br>4. 数据库中数据已更新 |
| **自动化脚本** | `test/automation/integration/user.crud.spec.ts` |

### USR-008 - 删除用户

| 项目 | 内容 |
|------|------|
| **用例 ID** | USR-008 |
| **用例名称** | 删除用户 |
| **优先级** | P0 |
| **前置条件** | 1. 管理员已登录<br>2. 用户已存在且可删除 |
| **测试数据** | 用户 ID |
| **测试步骤** | 1. 发送请求 `DELETE /api/system/users/:id`<br>2. 携带有效 Token<br>3. 传入要删除的用户 ID |
| **预期结果** | 1. 删除成功<br>2. 响应状态码 200 或 204<br>3. 数据库中用户记录被删除<br>4. 再次查询该用户返回 404 |
| **自动化脚本** | `test/automation/integration/user.crud.spec.ts` |

### USR-009 - 启用/禁用用户

| 项目 | 内容 |
|------|------|
| **用例 ID** | USR-009 |
| **用例名称** | 启用/禁用用户 |
| **优先级** | P1 |
| **前置条件** | 1. 管理员已登录<br>2. 用户已存在 |
| **测试数据** | `{enabled: false}` 或 `{enabled: true}` |
| **测试步骤** | 1. 发送请求 `PATCH /api/system/users/:id`<br>2. 携带有效 Token<br>3. 请求体设置 enabled 字段<br>4. 提交更新请求 |
| **预期结果** | 1. 状态更新成功<br>2. 响应状态码 200<br>3. 用户 enabled 状态已变更<br>4. 禁用用户无法登录 |
| **自动化脚本** | `test/automation/integration/user.crud.spec.ts` |

---

## 3. 角色管理测试 (ROLE-001 ~ ROLE-005)

### ROLE-001 - 创建角色成功

| 项目 | 内容 |
|------|------|
| **用例 ID** | ROLE-001 |
| **用例名称** | 创建角色成功 |
| **优先级** | P0 |
| **前置条件** | 1. 管理员已登录<br>2. 角色编码未被使用 |
| **测试数据** | `{code: "test_role", name: "测试角色", description: "测试用途"}` |
| **测试步骤** | 1. 发送请求 `POST /api/system/roles`<br>2. 携带管理员 Token<br>3. 请求体包含角色信息<br>4. 提交创建请求 |
| **预期结果** | 1. 角色创建成功<br>2. 响应状态码 201<br>3. 返回角色信息<br>4. 数据库中新增角色记录 |
| **自动化脚本** | `test/automation/integration/role.crud.spec.ts` |

### ROLE-002 - 创建角色失败 - 编码重复

| 项目 | 内容 |
|------|------|
| **用例 ID** | ROLE-002 |
| **用例名称** | 创建角色失败 - 编码重复 |
| **优先级** | P0 |
| **前置条件** | 1. 管理员已登录<br>2. 角色编码已存在 |
| **测试数据** | `{code: "admin", name: "重复管理员"}` |
| **测试步骤** | 1. 发送请求 `POST /api/system/roles`<br>2. 使用已存在的角色编码 `admin`<br>3. 提交创建请求 |
| **预期结果** | 1. 创建失败<br>2. 响应状态码 409<br>3. 返回错误信息"角色编码已存在" |
| **自动化脚本** | `test/automation/integration/role.crud.spec.ts` |

### ROLE-003 - 查询角色列表

| 项目 | 内容 |
|------|------|
| **用例 ID** | ROLE-003 |
| **用例名称** | 查询角色列表 |
| **优先级** | P0 |
| **前置条件** | 1. 管理员已登录<br>2. 系统中存在角色数据 |
| **测试数据** | 无 |
| **测试步骤** | 1. 发送请求 `GET /api/system/roles`<br>2. 携带有效 Token<br>3. 获取角色列表 |
| **预期结果** | 1. 查询成功<br>2. 响应状态码 200<br>3. 返回角色列表数组 |
| **自动化脚本** | `test/automation/integration/role.crud.spec.ts` |

### ROLE-004 - 更新角色信息

| 项目 | 内容 |
|------|------|
| **用例 ID** | ROLE-004 |
| **用例名称** | 更新角色信息 |
| **优先级** | P0 |
| **前置条件** | 1. 管理员已登录<br>2. 角色已存在 |
| **测试数据** | `{name: "更新后的角色名", description: "新描述"}` |
| **测试步骤** | 1. 发送请求 `PATCH /api/system/roles/:id`<br>2. 携带有效 Token<br>3. 请求体包含要更新的字段<br>4. 提交更新请求 |
| **预期结果** | 1. 更新成功<br>2. 响应状态码 200<br>3. 返回更新后的角色信息 |
| **自动化脚本** | `test/automation/integration/role.crud.spec.ts` |

### ROLE-005 - 删除角色

| 项目 | 内容 |
|------|------|
| **用例 ID** | ROLE-005 |
| **用例名称** | 删除角色 |
| **优先级** | P0 |
| **前置条件** | 1. 管理员已登录<br>2. 角色已存在且未关联用户 |
| **测试数据** | 角色 ID |
| **测试步骤** | 1. 发送请求 `DELETE /api/system/roles/:id`<br>2. 携带有效 Token<br>3. 传入要删除的角色 ID |
| **预期结果** | 1. 删除成功<br>2. 响应状态码 200 或 204<br>3. 数据库中角色记录被删除 |
| **自动化脚本** | `test/automation/integration/role.crud.spec.ts` |

---

## 4. 权限验证测试 (PERM-001 ~ PERM-004)

### PERM-001 - 有权限访问接口

| 项目 | 内容 |
|------|------|
| **用例 ID** | PERM-001 |
| **用例名称** | 有权限访问接口 |
| **优先级** | P0 |
| **前置条件** | 1. 用户已登录<br>2. 用户角色拥有该接口权限 |
| **测试数据** | 有效 Token，有权限的接口 URL |
| **测试步骤** | 1. 使用有权限的用户 Token<br>2. 访问受保护的接口 `GET /api/system/users`<br>3. 携带 Authorization 头 |
| **预期结果** | 1. 访问成功<br>2. 响应状态码 200<br>3. 返回接口数据 |
| **自动化脚本** | `test/automation/integration/permission.access.spec.ts` |

### PERM-002 - 无权限访问接口

| 项目 | 内容 |
|------|------|
| **用例 ID** | PERM-002 |
| **用例名称** | 无权限访问接口 |
| **优先级** | P0 |
| **前置条件** | 1. 用户已登录<br>2. 用户角色没有该接口权限 |
| **测试数据** | 有效 Token，无权限的接口 URL |
| **测试步骤** | 1. 使用无权限的用户 Token<br>2. 访问受保护的接口<br>3. 携带 Authorization 头 |
| **预期结果** | 1. 访问被拒绝<br>2. 响应状态码 403<br>3. 返回错误信息"禁止访问" |
| **自动化脚本** | `test/automation/integration/permission.access.spec.ts` |

### PERM-003 - 未登录访问接口

| 项目 | 内容 |
|------|------|
| **用例 ID** | PERM-003 |
| **用例名称** | 未登录访问接口 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试数据** | 无 Token |
| **测试步骤** | 1. 不携带 Authorization 头<br>2. 访问受保护的接口<br>3. 发送请求 |
| **预期结果** | 1. 访问被拒绝<br>2. 响应状态码 401<br>3. 返回错误信息"未授权" |
| **自动化脚本** | `test/automation/integration/permission.access.spec.ts` |

### PERM-004 - Token 过期访问接口

| 项目 | 内容 |
|------|------|
| **用例 ID** | PERM-004 |
| **用例名称** | Token 过期访问接口 |
| **优先级** | P1 |
| **前置条件** | 1. 用户曾登录获取 Token<br>2. Token 已过期 |
| **测试数据** | 过期的 JWT Token |
| **测试步骤** | 1. 使用过期的 Token<br>2. 访问受保护的接口<br>3. 携带 Authorization 头 |
| **预期结果** | 1. 访问被拒绝<br>2. 响应状态码 401<br>3. 返回错误信息"Token 已过期" |
| **自动化脚本** | `test/automation/integration/permission.access.spec.ts` |

---

## 测试数据统计

| 模块 | 用例数 | P0 | P1 |
|------|--------|----|----|
| 登录/登出 | 5 | 4 | 1 |
| 用户 CRUD | 9 | 7 | 2 |
| 角色管理 | 5 | 4 | 1 |
| 权限验证 | 4 | 3 | 1 |
| **合计** | **17** | **14** | **3** |

---

**文档结束**
